{
  "name": "smart-attendance-system-monorepo",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build:all": "cd backend && npx prisma generate && npm run build && cd ../frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
